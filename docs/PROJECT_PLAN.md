# Spike-Trace 项目规划与开发上下文

独立验证来源契约由 `src/spiketrace/validation_contract.py` 统一维护：视频以相对仓库路径、SHA-256
和元数据冻结，验证清单与选择 JSON 仅扫描显式传入的来源并 fail-closed 防止内容重叠。

> 文档状态：已确认的项目基线  
> 更新日期：2026-08-30
> 用途：保存产品决策、MVP 边界和技术路线，供项目成员及后续 AI 持续开发时使用。

## 1. 项目愿景

Spike-Trace 最终应成为一个可以持续升级的排球比赛视频分析软件，而不是一次性脚本。

用户导入一场排球比赛视频后，软件应识别我方球员的技术动作，将事件归属到球衣号码，并长期保存比赛、球员和统计数据。用户可以在软件中查看及修正数据，也可以导出 CSV、XLSX、JSON 或其他格式。

长期需要支持的主要指标包括：

- 发球成功率与发球得分率。
- 球员每场及每局得分。
- 球员上场时间。
- 得分来源，包括扣球、拦网和发球。
- 一传次数、一传到位率和一传等级。
- 扣球次数、扣球得分和扣球成功率。
- 按我方球衣号码汇总的单场及跨比赛数据。

## 2. 当前最重要的产品决策

第一阶段优先开发核心动作模型，应用界面、完整数据库、报表和账号等产品功能暂时后置。

MVP 的目标不是宣称已经实现高准确率全自动统计，而是跑通一个可训练、可推理、可评估的动作识别闭环：

```text
标注视频 -> 训练动作模型 -> 输入完整视频 -> 滑窗推理
         -> 合并动作事件 -> 输出时间戳、类别、置信度 -> 人工检查
```

动作模型必须能够通过新增标注数据继续训练和替换。模型输出需采用稳定的数据契约，避免后续加入号码识别、数据库或界面时推倒重做。

## 3. MVP 范围

### 3.1 MVP 必须完成

1. 读取常见格式的本地比赛视频。
2. 获取视频帧率、分辨率、时长等元数据。
3. 按固定长度及重叠比例生成视频片段。
4. 根据动作标注训练视频动作分类模型。
5. 对完整视频执行滑动窗口推理。
6. 将相邻、同类且满足阈值的窗口合并为动作事件。
7. 输出事件 JSON 和 CSV，至少包含动作类型、起止时间、置信度和模型版本。
8. 提供训练集、验证集指标和混淆矩阵。
9. 保存训练配置、标签表和模型权重，保证结果可以复现。

### 3.2 MVP 暂不要求

- 完整桌面端或 Web 端界面。
- 用户账号、云同步或多人协作。
- 正式比赛数据库和球员档案页面。
- Excel/PDF 报表排版。
- 全自动球衣号码 OCR 和跨遮挡身份重识别。
- 精确的换人、轮转和上场时间统计。
- 无人工确认即可用于正式技术统计的准确率。

这些功能属于后续产品阶段，但动作模型的输出格式需要为它们预留接口。

## 4. 第一版动作标签

建议第一版使用以下互斥标签：

| 标签 | 含义 | 第一版识别目标 |
| --- | --- | --- |
| `background` | 无目标动作、等待或普通移动 | 必须 |
| `serve` | 发球动作 | 必须 |
| `receive` | 我方直接接对方发球的第一次触球 | 必须 |
| `set` | 二传组织动作 | 必须 |
| `attack` | 扣球、吊球等进攻击球 | 必须 |
| `block` | 拦网动作 | 必须 |
| `dig` | 针对对方进攻击球的防守起球，不含拦网 | 必须 |

对方直接出界且我方没有触球时标为 `background`。非进攻的被动送球在复核权威层保存为
`free_ball`，它是动作值而不是栏目，也不计入 `attack`。权威标签和训练投影必须分离：当前七类
动作模型不扩头，直接可见的 `free_ball` 投影为训练 `background`，但权威结果仍保存
`free_ball`；是否失误由独立的结果证据决定，不能由动作标签自动推断。

以下结果不应在数据不足时强行合并到第一版动作分类器中：

- 动作是否直接得分。
- 一传是否到位。
- 扣球是否出界或被拦。
- 发球是否为 ACE。

上述结果往往需要更长时间上下文、球轨迹、比分变化或多个球员之间的关系。后续可使用独立结果模型或事件规则引擎判断。

## 5. 输入视频基线

第一版训练和验收应尽量控制视频条件：

- 固定或相对稳定的全场机位。
- 最低 1080p，推荐 50 或 60 FPS。
- 球网和双方半场大部分时间可见。
- 避免大量回放、特写、字幕遮挡和频繁剪辑。
- 训练集和验证集必须按比赛划分，不能把同一场比赛的相邻片段随机拆到两边，否则评估结果会虚高。

不同机位、分辨率和比赛级别应记录为数据集元信息，便于后续定位模型在哪些场景失效。

## 6. 标注规范

每条标注至少需要包含：

```csv
video_path,start_seconds,end_seconds,label,team_side,player_number,crop_x1,crop_y1,crop_x2,crop_y2,split
videos/match_001.mp4,12.40,13.20,serve,far,8,0,0,1280,430,train
videos/match_001.mp4,16.20,17.00,receive,far,3,0,0,1280,430,train
videos/match_010.mp4,28.50,29.30,attack,near,12,0,170,1280,720,val
```

其中 `team_side` 和 `player_number` 可以为空。`crop_x1` 至 `crop_y2` 用于保留目标球队半场，四项应全部填写或全部留空。真实视频验证表明，同一全场画面可能同时出现我方拦网和对方扣球，因此首版训练应优先使用我方半场裁剪，并将动作中心放在约 0.8-1.0 秒窗口内。

标注时需要统一动作起止边界：

- 发球：从明显准备动作开始，到击球后的随挥结束。
- 接发球：从球员响应对方发球开始，到第一次触球后的动作恢复。
- 防守起球：从球员响应对方进攻开始，到触球后的动作恢复；拦网触球仍归 `block`。
- 二传：从准备触球开始，到传球动作结束。
- 扣球：从助跑或明显起跳准备开始，到落地或随挥结束。
- 拦网：从移动并准备起跳开始，到落地结束。
- 同一片段中存在多个主要动作时，第一版优先切成较短的单动作片段。

必须包含足够多的 `background` 负样本，包括普通跑动、庆祝、暂停、捡球和裁判画面，否则模型会过度报出动作。

## 7. 模型与推理架构

### 7.1 第一版模型

动作模型采用可插拔双路径，先复用公开模型验证可迁移性，同时保留自主训练能力：

- 自训练基线：`R3D-18`，结构简单，便于持续吸收本项目的人工修正数据。
- 预训练候选：直接加载六类 YOLO 排球动作权重，将 `spike` 映射为 `attack`，忽略 `ball`；不复制第三方仓库代码。
- 后续候选：VideoMAE、SlowFast 或轻量级时序 Transformer。
- 预训练权重先在现有标注窗口上评估；当前外部 YOLO 入口不会扫描整场，只有召回率达到可用水平且补齐整场候选生成入口后，才能用于候选片段发现和预标注。
- 球员跟踪、我方身份和球衣号码识别仍不是动作分类 MVP 的硬依赖。

内部训练、清单和严格指标使用稳定七类契约：`background, serve, receive, set, attack, block, dig`。上游 YOLO 保持六类能力，不要求旧权重提供 `dig`。评估同时输出严格七类 `metrics`，以及仅为比较旧模型宽泛防守触球候选能力而将 `dig -> receive` 的六类 `compatibility_metrics`；兼容指标不能覆盖人工标签或作为七类部署成绩。所有路径仍需保留置信度、模型来源和检测证据，外部项目公布的指标不能代替本项目按完整比赛隔离的评估。

### 7.2 推理过程

1. 视频解码并按目标帧率采样。
2. 使用固定长度窗口，例如 32 帧或 64 帧。
3. 相邻窗口保持重叠，减少动作落在边界时的漏检。
4. 模型为每个窗口输出各动作类别概率。
5. 丢弃低于类别阈值或判断为 `background` 的窗口。
6. 合并时间相邻且类别相同的窗口。
7. 对持续时间过短的孤立结果进行过滤。
8. 输出最终事件及推理证据。

### 7.3 事件输出契约

```json
{
  "video_id": "match_001",
  "event_id": "evt_000123",
  "start_ms": 12400,
  "end_ms": 14100,
  "action": "serve",
  "confidence": 0.91,
  "team_side": null,
  "player_number": null,
  "status": "predicted",
  "model_version": "action-r3d18-v0.1",
  "source": "sliding_window"
}
```

后续号码识别模块以多对多 `action_participants` 关系保存实际参与者、参与类型、触球状态和确认
证据，不覆盖动作模型输出。只有一个动作恰好存在一名已确认参与者时，才可向兼容字段
`player_number` 投影该号码；多人拦网或身份未确认时保持为空。无效拦网只属于实际参与者，
不能通过直接修改 `ActionEvent.player_number` 或根据动作标签推断归属。

## 8. MVP 验收标准

程序链路验收和模型质量验收必须分开。

### 8.1 程序链路验收

- 能在目标设备上完成训练和推理命令。
- 对损坏视频、空标注和未知标签给出明确错误。
- 同一配置和模型权重产生可复现结果。
- 能生成合法的 JSON、CSV 和评估结果。
- 长视频处理过程可以显示进度，并允许失败后定位原因。

### 8.2 模型质量验收

- 使用从未参与训练的整场比赛作为验证集。
- 主要指标使用每类 Precision、Recall、F1 和混淆矩阵。
- 同时评估事件级指标，不能只报告片段分类准确率。
- 第一轮不预设不现实的高准确率目标；先建立基线，再根据真实数据确定目标。
- 重点观察 `attack` 与 `block`、`receive` 与 `set` 等易混类别。

## 9. 只统计我方和号码归属的后续方案

动作模型跑通后，再按以下顺序加入身份归属：

1. 检测球员、球网和场地，获得球员轨迹与所在半场。
2. 每局开始时由用户确认我方半场，换边后更新。
3. 使用人物跟踪维持短时间轨迹。
4. 从轨迹中选择号码清晰的正面或背面帧执行 OCR。
5. 对多帧识别结果进行置信度加权投票。
6. 将候选号码限制在赛前录入的合法名单中。
7. 使用场上人数、号码唯一性、轮转位置和自由人等阵容约束排错。
8. 将低置信度结果交给人工确认。

仅依靠逐帧 OCR 无法稳定完成号码归属。遮挡后的重新绑定还需要外观特征、位置连续性和阵容状态共同判断。

## 10. 长期产品数据架构

动作模型稳定后，软件采用本地优先的数据架构：

- SQLite 保存比赛、球员、阵容、轨迹、事件、审核记录和统计结果。
- 视频和证据片段保存在文件系统，数据库只保存路径、哈希和元数据。
- 原始预测与人工修正同时保留，不直接覆盖。
- 每条事件记录模型版本和置信度。
- 统计指标从事件数据重新计算，而不是只保存不可追溯的汇总数字。
- CSV、XLSX、JSON 和 PDF 是导出格式，不是内部唯一数据源。
- 后续可以提供项目归档格式，例如 `.spiketrace`，用于跨设备备份和恢复。

长期界面可包含比赛资料库、分析任务、校对工作台、比赛报告、球员档案、导出中心以及模型管理，但它们不属于当前动作模型 MVP。

## 11. 长期统计口径草案

以下定义需要在进入正式统计功能开发前由使用者最终确认：

| 指标 | 建议公式 |
| --- | --- |
| 发球成功率 | 发球进入有效回合次数 / 发球总次数 |
| 发球得分率 | ACE 次数 / 发球总次数 |
| 个人得分 | 扣球得分 + 拦网得分 + 发球得分 |
| 扣球成功率 | 扣球直接得分 / 扣球尝试次数 |
| 一传到位率 | 一传到位次数 / 有效一传次数 |
| 上场时间 | 球员进入场上至被换下的有效比赛时间累计 |

一传建议保留 0-3 级评分，而不只保存“到位/不到位”。对方失误得分原则上不归入任何我方球员的个人得分。

## 12. 推荐开发阶段

### 阶段 A：动作模型闭环，当前优先级

- 建立标注规范和数据集清单。
- 实现视频片段加载、训练、验证和整场推理。
- 建立 R3D-18 基线模型。
- 接入可选 YOLO 预训练动作检测器，生成兼容性指标和人工复核表。
- 输出动作事件 JSON/CSV。
- Rangitoto 已完成的 40 段、83 条人工记录已发布为 v2 分层结果和训练投影；推断或不可见动作
  只保留为比赛事实，不进入视觉训练。
- 已用真实比赛建立第一份同场零样本基线报告；独立验证基线仍待第二场比赛。

### 阶段 B：增强事件理解

- 加入球员、球和场地检测。
- 从动作分类升级到时序动作定位。
- 结合比分、球轨迹和动作顺序判断动作结果。
- 支持发球是否成功、扣球是否得分和一传等级。

### 阶段 C：我方与号码归属

- 球员跟踪、我方半场判断和换边处理。
- 多帧号码 OCR、轨迹绑定和阵容约束。
- 为低置信度身份提供人工确认。

### 阶段 D：完整软件产品

- 本地比赛资料库和数据库迁移。
- 视频校对工作台。
- 按比赛、局次和球员统计。
- CSV、XLSX、JSON、PDF 导出。
- 数据备份、恢复和模型升级。

## 13. 当前数据状态和下一步输入

已经加入第一场真实比赛用于开发：2024 奥运会美国队对德国队。本项目将美国队（USA）
定义为我方；本场美国队穿深蓝色球衣。源视频为 1280x720、30 FPS，时长约 2 小时
8 分钟，使用较稳定的全场端线机位。

Rangitoto 第二场完整比赛的首轮主动学习已经完成：40 个短片共有 83 条人工记录，原
`review.xlsx` 和选择 JSON 保持不可变；用户不需要再填写工作簿或运行 v1 提取。v2 输入为
`data/active-learning/rangitoto/round-01-review-v2.json`，并已发布不可覆盖的六文件目录
`data/annotations/rangitoto_round_01/`。权威 JSON 记录 86 条动作观察、4 条结果观察、1 条连续遮挡、
2 条镜头外事件、3 个受影响动作和 0 条已确认参与者；训练投影含 60 条正样本、60 条背景和 214 行。
权威内容 SHA-256 为 `6dd0b1ab2dae0481fbdb55a2c7afd1c9937024cf13a0143d0e8cd5877dc7182f`，训练 CSV 的
SHA-256 为 `2c0ff1900c74622cf99f07527b66fc48b6e18f0462f393234624cd9719012ac6`，六文件 manifest 的
文件 SHA-256 为 `af17ed6f5358d01633d90c003f8127d50a0a60a72fd57d6c6f09e849c2f2a0d4`。

动作证据与结果证据独立保存。连续遮挡和镜头外是不同的持续时间事件；其保护区间不得产生假阴性。
参与者输出必须在身份确认前保持为空。一个 `block` 无论有多少参与者，都只产生一个训练窗口；
参与关系绝不向整支队伍广播。旧 v1 应用器和提取工具保留兼容，新的权威发布使用
`apply-active-review-v2` 的单一输出目录，并通过 `verify-active-review-bundle --repo-root .`
对冻结选择、合并候选、保护区间和硬负样本执行来源绑定的只读重放验证。

已建立 `728.0-735.0` 秒的首个试标注区间。该区间美国队位于远端，使用
`(0, 0, 1280, 430)` 裁剪，只保留美国队半场；近端半场建议使用
`(0, 170, 1280, 720)`。67 个候选窗口已完成首轮人工复核，46 条标签被修正；该清单作为
`first_pass_reviewed` 历史输入继续保留。首轮包含 45 个 `background`、8 个 `block`、6 个
`receive`、5 个 `serve`、2 个 `set` 和 1 个 `attack`。

内部动作契约已升级为七类。17 条二次复核已完成并应用，规格和机器可读结果分别为
`data/annotations/usa_germany_2024_second_review.json` 与
`data/annotations/usa_germany_2024_second_review_results.json`。复核覆盖以下原清单记录：

```text
1, 19, 21, 22, 23, 27, 31, 32, 35, 39, 43, 46, 47, 53, 65, 66, 67
```

结果原位更新 16 条并新增 1 条：记录 46 的原 `set` 保留，另追加 `attack 4026-4027`。
当前二次复核来源清单共 68 条、总窗口时长 60.3 秒，包含 39 个
`background`、9 个 `serve`、3 个 `receive`、2 个 `set`、2 个 `attack`、9 个 `block`
和 4 个 `dig`，全部属于 `train`。首轮清单未被覆盖，原备注和二次确认均可追溯。
复核应用器精确匹配来源快照，沿用来源清单的视频根校验相对路径，并在临时文件完整
验证后原子替换输出；任何异常都不会破坏已有结果。

当前扩展状态、兼容性基线与下一步：

2026-08-07 已用上游六类 YOLO 权重在 68 条二次复核清单上完成历史基线：阈值 0.25、
每窗口 6 帧、CPU 环境下，严格七类 Accuracy 为 0.676471（46/68）、Macro F1 为
0.353654，六类兼容 Macro F1 为 0.368151。

67 条首轮清单的 Accuracy 0.776119、Macro F1 0.491001 仅保留为历史记录；二次复核改变了
标签、时间窗并新增 1 条记录，两组数字不能当作代码或模型回归比较。这些结果仍只来自同一场
比赛，不是独立验证或部署成绩。机器可读参数、权重和清单哈希、逐类指标已写入
`data/annotations/usa_germany_2024_match.json` 的 `pretrained_baseline`。

第一批完整回合穷举补标已经建立为
`data/annotations/usa_germany_2024_expansion_batch_01.json`。批次锁定当前 68 条清单的
SHA-256，按优先级覆盖已知区间 19、13、11、2、6、14，共 70.6 秒。区间 19 的 7 条
现有窗口全是 `background`，但画面是包含多次防守和转换的长回合；其余区间重点检查
未覆盖的 `set/attack/dig/block` 和时间边界。用户只需完整播放区间、确认已看完，并以
`HH:MM:SS` 秒级时间补录缺少的美国队动作。区间 4、10 和 20 经独立画面抽样后降级：
它们没有可确认的新美国队触球，或已有动作已经覆盖。

填写工作簿固定为
`outputs/expansion-batch-01/usa_germany_2024_expansion_batch_01.xlsx`，作为 `outputs/` 下明确
纳入版本控制的人工结果入口，保证换设备后仍能继续。填完后保留原路径提交；本批次已将
非空动作规范化为带来源快照的结果 JSON，并生成新的训练清单，避免人工直接编辑 CSV。

2026-08-09 已完成第一批：6/6 个完整回合确认。动作页没有非空行，但人工在“完整回合”备注
中明确记录了 R19 的 6 个动作和 R06 的 2 个 `block`，已规范化为
`data/annotations/usa_germany_2024_expansion_batch_01_results.json`，并应用到
`data/annotations/usa_germany_2024_annotations_expanded_batch_01.csv`。扩展清单共 73 条、
64.7 秒，新增 `receive` 1、`set` 2、`attack` 1、`block` 3、`dig` 1，并移除重复的
`block` 2、`dig` 1，净增 5 条；工作簿 SHA-256、来源单元格、删除与保留记录快照均保留。
R19 的“没碰到球” attack 不计入动作；不可变的 68 条来源清单没有被覆盖。

2026-08-09 已用相同权重、版本、CPU 设备和参数在 73 条扩展清单上重跑。严格七类
Accuracy 为 0.630137（46/73）、Macro F1 为 0.344159，六类兼容 Macro F1 为
0.366886；逐类 Recall 为 `background` 94.9%、`serve` 22.2%、`receive` 50.0%、
`set` 25.0%、`attack` 0%、`block` 40.0%、`dig` 0%。主要错分为
`serve -> background` 7 条、`block -> background` 3 条、`block -> set` 3 条。
共同 65 个窗口的预测没有变化；移除的 3 条重复窗口和新增 8 条窗口各包含 1 条正确预测，
因此正确数保持 46，Accuracy 下降 0.046334。差异来自数据构成变化，不是模型代码回归；
完整当前指标、68 条历史摘要、产物哈希和对比均已写入比赛元数据。

2026-08-09 已在比赛元数据的 `usa_side_segments` 中加入并由用户确认 6 段美国队主动比赛区间。
第 1 至第 4 局依次为 `far / near / far / near`；第 5 局按换边拆为 `near 6711-7111`
和 `far 7194-7675`，中间转换空档不归入任何一段。每段记录唯一 `segment_id`、局次和阶段、
秒级起止时间、`usa_side`、裁剪坐标、确认状态、置信度、视觉证据、
`boundary_semantics: inclusive_candidate` 以及 1 秒边界容差，并显式保留
`confirmation_source: user` 和 `confirmed_on: 2026-08-09`。6 段均按时间排序、不重叠、
位于视频时长内，并以相同半场唯一覆盖现有 20 个复核区间。这些边界来自关键边界抽帧视觉检查，
精度为 1 秒，可用于后续扫描裁剪和人工标注，但不能声称为精确裁判记录或完整比赛统计。

第二批完整回合补标已于 2026-08-10 应用：规格为
`data/annotations/usa_germany_2024_expansion_batch_02.json`，来源是第一批 73 条训练清单
`usa_germany_2024_annotations_expanded_batch_01.csv`，来源 SHA-256 为
`2ad5ce8d46e5f065c76d82d641f0fb824f5e8da9457df7bde5466d110cc12981`。每个已确认半场段选取一个
新的完整回合，合计 6 段、85 秒、每段 8 个动作槽位：

| 批次编号 | 来源段 | 时间 | USA 位置 |
| --- | --- | --- | --- |
| `B02-R01` | `set_1` | `00:13:57-00:14:06` | `far` |
| `B02-R02` | `set_2` | `00:39:35-00:39:47` | `near` |
| `B02-R03` | `set_3` | `01:10:09-01:10:22` | `far` |
| `B02-R04` | `set_4` | `01:34:10-01:34:29` | `near` |
| `B02-R05` | `set_5_pre_switch` | `01:52:56-01:53:14` | `near` |
| `B02-R06` | `set_5_post_switch` | `02:02:58-02:03:12` | `far` |

已填写并纳入版本控制的工作簿为
`outputs/expansion-batch-02/usa_germany_2024_expansion_batch_02.xlsx`，SHA-256 为
`6054c3abc54d8ac1dda7cdfec0a8297501e6aee63c2239d4966b962529180a58`。6/6 个完整回合均已确认，
“新增动作”页为空；从“完整回合”备注规范化新增 17 个美国队动作。`B02-R05` 的
`background`（对手失误）仅作为忽略备注，不新增美国队动作。`B02-R04` 同秒的 `set`/快攻经原视频
0.1 秒密集抽帧细化为 `set 5655.9-5656.5`、`attack 5656.6-5657.2`，两个窗口不重叠。

结果为 `data/annotations/usa_germany_2024_expansion_batch_02_results.json`，输出清单为
`data/annotations/usa_germany_2024_annotations_expanded_batch_02.csv`，输出 SHA-256 为
`17fab8ffa9bf6d77c896491aa7da7e15920679ff9bda8cbb207259e89815ef2d`。当前训练清单共 90 条、80.9 秒，
分布为 `attack` 8、`background` 39、`block` 12、`dig` 6、`receive` 6、`serve` 11、`set` 8，全部属于
`train`；原 73 条第一批清单保持不变。本场仍只有同一比赛数据，不能作为独立验证集。

外部预训练 YOLO 基线仍只对应第一批 73 条清单；第二批 90 条清单尚未重跑，不能混称，也不改变
既有历史 baseline 数值。

下一步严格按以下顺序执行：

1. 完成未触及的整场比赛验证真值（已具备 prediction-blind 草稿与不可变锁定 bundle 契约）。
2. 运行高效的两阶段微调。
3. 在选定回合实现身份跟踪和已确认的号码归属。
4. 加入 SQLite，以及用户复核、统计和导出工作流。

首轮实验建议准备至少 3-5 场目标机位的完整比赛。少量数据可以验证流程，但不能
代表泛化能力；后续需要持续加入不同场馆、光照、球衣、机位和比赛水平的数据。

## 14. 给后续开发者和 AI 的约束

- 当前优先级始终是动作模型训练和推理闭环，除非项目负责人明确调整。
- 不要在缺少独立验证集时声称模型达到可用准确率。
- 不要用随机输出或基于文件名的假结果替代模型推理。
- 先保持模块化单体结构，不要过早引入微服务。
- 模型、事件规则、号码归属和统计公式必须保持独立边界。
- 新功能不得破坏事件输出契约；需要变更时应进行版本化。
- 训练/验证/测试集必须按比赛隔离。
- 所有模型结果必须保留置信度、模型版本和来源信息。
- 人工修正数据是未来训练集的重要资产，后续产品不得丢弃修正历史。

独立验证回合队列模块已加入：它生成 prediction-blind 的运动候选，补齐整场回合/非回合覆盖，按已确认换边区间应用近端/远端裁剪，并写出不含音轨的静音代理与原子 manifest。
